# Phase 9 plan — measurement extension

Implementation plan for phase 9 of [the osr-forge spec](spec.md), the first phase of [the improvement arc](improvement-spec.md): the arc's yardstick, built before any extraction change ships. Three new metric families — doors, vertical transitions, the entrance — plus an encounter-precision metric riding a new asserted-empty truth convention, with the truth passes completed in-phase over every corpus member, the standing phase 7 sweep pair re-scored offline, the band table refreshed, and a contingent non-BFRPG committed member if a candidate passes the license procedure. The milestone: **door, transition, entrance, and encounter-precision quality are measured numbers on every corpus member — with noise bands on the committed corpus — before phases 10–12 touch a prompt.** The roadmap entry is already in the spec (improvement-arc PR #27); remaining spec impacts land with the implementation PR, as before.

Six facts shape everything:

- **The offline re-score works today, verified.** The retained phase 7 workdirs live at `~/osr-forge-measurement/phase7/` (a path recorded nowhere until now — this plan pins it): `sweep1-`/`sweep2-` pairs for all three committed members plus four BYOM workdirs, every one holding `run.json` and a complete `stages/` set. Re-scoring `sweep1-jn1-chaotic-caves.forge` against today's truth reproduces the committed scoreboard entry exactly (F1 0.6436, custom 0.2857, all four families) — so the existing families are a regression check on the scorer refactor: after this phase's changes, the re-score must reproduce them byte-for-byte, and any movement is a bug, not noise.
- **A cache-based door metric scores extraction, not geometry — and that seam is deliberate.** `score_workdir` reads stage caches only; geometry is synthesized in-memory at assembly and never cached. So the door family measures the *content pass's* door assertions (`via`, `door_locked` on `AreaConnection`), and geometry-only behaviors — the doors dropped on cycle-closing routes that phase 11 rescues — are invisible to it. The design answer is a seam, not a doctrine break: all edge facts (presence, via-class, door kind, locked) flow through one function over the caches, and phase 11 reroutes that function through its deterministic reconciliation — the phase 7 precedent (`usable_stat_block` imported into the scorer so the metric can never disagree with assembly's own predicate) applied to edges. Phase 9 pins the metric semantics; phase 11 swaps the fact source.
- **Transitions must be dungeon-scoped, not level-scoped.** The retained caches show three vertical wire shapes: a cross-level keyed link whose target key lives in a sibling level's cache (JN1 `roslof-manor` 85 ↔ 98), a level-shaped stub (`to_key: null, to_level: N`) that today's connection scorer explicitly skips, and — the shape that breaks any level-scoped design — a printed multi-level site the survey collapsed into one extracted level, so the printed inter-level stair reads as a *same-level* connection (JN2's Reed Marsh Temple: truth splits keys 1–8 / 9–14 across two levels; extraction has one level and `3 → 9, via: stairs, direction: up`). The metric therefore matches truth `(level, key) → (level, key)` endpoint pairs against extracted endpoints resolved through the pairing claims (which truth area each extracted key was matched to, and on which truth level), regardless of which level cache a connection came from. The measured stakes are real: JN2's sweep 1 has 27 stairs connections, zero with `to_level`, and an `adventure.json` with zero transitions across all six dungeons.
- **The entrance is derivable from `survey.json` alone.** Today's entrance is a pure positional heuristic — the first listed area of the lowest-numbered non-empty level (`geometry.py`) — reproducible in the scorer in a few lines with no content cache read. Truth supplies the printed answer; the metric scores the heuristic now (the pre-change baseline phase 11 needs) and the map-proposed entrance later through the same seam.
- **The asserted-empty flip is a live migration hazard.** `TruthArea.encounters` defaults to `()`, so an omitted key and `encounters: []` are indistinguishable today. Making the field `| None = None` silently flips every omitted key — 57 of JN1's 137 areas, 47 of JN2's 86, 1 of minimod's 6 — from "asserted none" to "unasserted." The committed truth passes therefore write explicit `encounters: []` on every verified-empty area (that verification *is* the pass), the full-assertion repo test extends to encounters, and the name-recall denominator keeps its meaning (listed truth encounters) by construction — JN1's 109 and JN2's 57 must not move for a non-quality reason.
- **The BYOM inventory is five members, four of them scoreable.** `hole-in-the-oak`, `b3-palace-of-the-silver-princess`, `b4-the-lost-city`, and `dcc-81-the-one-who-watches-from-below` have retained phase 7 workdirs and scored entries on the private corpus scoreboard (which exists, four entries — a fact the regeneration mechanism in work item 5 must survive); `aa1-adventure-anthology-one` has a manifest and truth file but no retained workdir and no scored entry (withdrawn by the owner, never publishes). AA1 still receives the truth pass: the truth-semantics standing obligation reads "every corpus member, committed and BYOM alike," its purpose is that no member's truth goes stale against the schema — exactly the debt an AA1 skip would create — and one bounded authoring-plus-verification pass is cheaper than carving a deviation from a standing rule. AA1 produces no number this phase, and that is recorded, not discovered.

## Scope

In scope:

- The truth-schema extension: `doors` and assertion-aware `encounters` on `TruthArea`, `transitions` and `entrance` on `TruthDungeon`, with validators that keep contradictions unrepresentable
- The scorer extension: the edge-fact seam, the door family, the dungeon-scoped transition family, the entrance family, encounter precision — plus the metric models, `corpus_means`, and `_print_metrics` plumbing
- The truth passes over every corpus member — committed (JN1, JN2, minimod) and BYOM (all five, AA1 included) — authored and adversarially verified under `AUTHORING.md`, provenance appended, the owner-sampling delta filed as a GitHub issue
- The offline `rescore` tooling — the mechanism the regression rule's "re-score the standing sweep pair" discipline has run on folklore until now
- The offline re-score of the standing pair, the refreshed band table with the new families' rows, the scoreboard regeneration, and the phase amendment recording the pair
- The contingent non-BFRPG committed member: candidate search under the phase 0 license procedure; on a pass, manifest + full truth + its own live double sweep (this phase's only live spend); on failure, the BF1-style record
- Docs: the README band table and conventions, `AUTHORING.md`'s new-family rules and the resolved asymmetry paragraph, `docs/evals.md`, glossary entries, the spec's family sentence, the changelog bullet

Out of scope, each with its disposition:

- **Stuck-door assertion.** JN1's caches record zero stuck doors, B3/B4/HotO none, and DCC-81 exactly one — the denominator is empty or near-empty everywhere outside JN2 and minimod. `TruthDoor` carries `kind` and `locked` only; stuck stays extractable (the cache field exists) and unmeasured until a member carries signal. Recorded, revisitable.
- **Direction-sense scoring on transitions.** Stairs are reciprocal and trapdoors/chutes one-way, but v1 matches undirected endpoint pairs with kind scored as an attribute; scoring the travel sense adds a truth convention (which end is "from") for near-zero discrimination on this corpus. Deferred with the kind vocabulary already in place.
- **A door-flag-on-non-door-via defect metric.** The caches show door conditions on non-door `via` values; geometry's posture is discard-with-flag, and the scorer mirrors it (such flags contribute no door fact). Counting them as a defect class is a diagnostic, not a quality metric; unpicked.
- **Geometry-realization measurement.** Whether a cached door fact survives into `adventure.json` edges is phase 11's territory via the seam; this phase scores extraction only. Named so nobody reads the door numbers as map fidelity.
- **Any extraction prompt or schema change.** The point of the phase. The fixture-re-record and live-sweep obligations do not fire (the contingent member's sweep is corpus growth, not an extraction change).

Spec impacts, applied with the implementation PR:

- **§ Testing and evals**: "four metric families" becomes the extended enumeration (areas, encounters — recall, count, resolution, custom, precision — connections, treasure, doors, transitions, entrance), with the asserted-empty encounters convention noted alongside the existing assertion-aware families.
- **§ Open questions**: if the contingent member joins, the corpus-diversity entry records it; if not, the entry gains the failure record pointer.

## Work items

### 1. The truth schema — `evals.py` models, validators, repo tests

- `TruthArea.encounters: tuple[TruthEncounter, ...] | None = None` — present asserts the complete encounter list (possibly empty), omitted asserts nothing. The committed full-assertion test (`test_committed_members_stay_fully_pinned_and_asserted`) extends: every committed area asserts encounters.
- `TruthDoor` (new): `kind: Literal["door", "secret_door"]`, `locked: bool = False`. `TruthArea.doors: dict[str, TruthDoor] | None = None`, keyed by neighbor printed key — present asserts the complete door-fact set over that area's asserted connections; omitted asserts nothing. Validators: `doors` requires `connections` asserted on the same area, and every `doors` key must appear in `connections` (slug-matched). A `TruthLevel` validator rejects contradiction on any undirected pair where *both* endpoints assert `doors` — and agreement covers presence, not just facts: endpoint A asserting a `doors` set that omits B while B's set lists A is the contradiction "implicit no-door vs explicit door," rejected the same as a kind or locked mismatch.
- `TruthTransition` (new): `from_level: int`, `from_key: str`, `to_level: int`, `to_key: str`, `kind: Literal["stairs", "trapdoor", "chute"]` — the kind vocabulary mirrors geometry's `_transition_via` narrowing. `TruthDungeon.transitions: tuple[TruthTransition, ...] | None = None` — present asserts the dungeon's complete vertical-link set; each endpoint's level must exist and its key must slug-match an area on that truth level (validator). One entry per link, endpoint order free (matching is undirected).
- `TruthEntrance` (new): `level: int`, `key: str` (same existence validators). `TruthDungeon.entrance: TruthEntrance | None = None`.
- The committed-corpus assertion posture, pinned and test-enforced in full: every committed area asserts `encounters` and (as today) `treasure`; `doors` asserted on every area whose `connections` are asserted; `transitions` and `entrance` asserted on every committed dungeon. `test_committed_members_stay_fully_pinned_and_asserted` enforces all of it — its stated purpose is that the gating corpus never thins by accident, and a new family that can thin without a failure isn't enforced. Partial assertion stays the BYOM posture. The same-level rule for `connections` is untouched — vertical links live only in `transitions`, and `test_truth_connections_reference_same_level_keys` stands.

### 2. The scorer — the edge-fact seam and four new computations

- **The seam.** One function derives per-dungeon undirected edge facts from the caches — endpoints (matched slugs), via-class, door kind, locked — and both the existing connection F1 and the new door family consume it. Dedup rule for the two directed mentions of one edge, pinned: door presence if either side states a door via; kind conflict resolves to `secret_door` (the more specific claim); locked if either side states it; door conditions on non-door via contribute nothing (geometry's discard posture). The connection F1's semantics are byte-identical through this refactor — the standing-pair re-score proves it.
- **Doors** (`DoorMetrics`): universe = undirected edges between matched areas where at least one endpoint asserts `doors`, restricted (like connections) to edges with an asserted-connections endpoint. Counts: `truth_doors`, `extracted_doors`, `true_positives`, `recall`, `precision`; on true positives, `kind_matched`/`kind_accuracy` and `locked_matched`/`locked_accuracy`. House style throughout: counts first, `_ratio` rounding, `None` on empty denominators.
- **Transitions** (`TransitionMetrics`): a per-dungeon pass after the level loop, over dungeons whose truth asserts `transitions` (assertion-aware on both sides, exactly like connections). Endpoint resolution never uses a truth-slug map (per-level key reuse — a dungeon keying every level 1..N — would make one ambiguous): the *source* endpoint's truth `(level, key)` comes from the pairing that matched the source area, and a keyed *target* resolves through the dungeon-wide claim registry — it resolves iff some pairing matched that extracted key to a truth area, and its truth level is that pairing's. The classifier over a matched area's connections, ordered and total:
    1. `to_level` set and `to_key` null → a vertical claim in stub form. It matches a truth transition on the source endpoint plus target level *number* compared directly (both are printed level numbers; the alignment map is not involved) — the landing key is geometry's guess policy, not extraction's claim. A mention carrying *both* `to_key` and `to_level` (JN1's real stair does) takes the keyed path below, falling back to stub form only if the key doesn't resolve.
    2. Otherwise, a keyed target that resolves through the claim registry → a vertical claim iff the target's truth level differs from the source's *and* the mention carries a vertical signal (via ∈ {stairs, trapdoor, chute} *or* direction ∈ {up, down} — mirroring geometry, which treats an up/down direction as vertical regardless of via). A resolving target on the *same* truth level is a same-level edge and only that, whatever its via — one extraction claim is never scored in two families. A different-truth-level target with no vertical signal is neither: extraction made a same-level claim the F1 machinery already ignores (the target is unmatched within that pairing), and crediting it as a transition claim would score a claim extraction didn't make.
    3. Otherwise, a keyed target that resolves nowhere → dropped from the universe, mirroring the connection F1's conservatism (an unmatched target never becomes a false positive; on partial BYOM truth it usually means uncovered pages, not hallucination).
    4. No target of any kind (`to_key` and `to_level` both null) → dropped, mirroring geometry's `no target stated` discard.

    Claims then deduplicate before any counting, keyed by the undirected endpoint pair (pair-form claims) or by `(source endpoint, target level)` (stub-form claims) — reciprocal and repeated mentions merge (JN1's one real link appears as three mentions; JN2's Reed Marsh pair as four), and a stub-form claim merges into a pair-form claim of the same physical link (same source endpoint, pair far endpoint on the stub's target level; the richer pair form wins). Kind is a resolved *attribute* of the merged link, not part of its identity: per mention it is `via` narrowed exactly as geometry's `_transition_via` does (trapdoor and chute as themselves, everything else stairs), and conflicts across merged mentions resolve by the pinned total order trapdoor > chute > stairs (arbitrary but deterministic; conflicts are vanishingly rare). `kind_accuracy` compares the resolved attribute against truth on true positives — matching is by endpoints alone, so the kind row can genuinely miss. Keyed-target lookup order, pinned: a claim on the source's own extracted level first, then sibling pairings in survey order — mirroring geometry's resolve-locally-then-on-siblings. Counts: `asserted_dungeons`, `truth_transitions`, `extracted_transitions` (deduplicated links), `true_positives`, `recall`, `precision`, `kind_matched`/`kind_accuracy` on true positives.
- **Entrance** (`EntranceMetrics`): per aligned dungeon whose truth asserts `entrance` — reproduce geometry's selection from `SurveyIndex` (lowest-numbered non-empty level, first listed area), match when the truth key slug-matches the selected area's key, the truth level aligns to the selected extracted level, and the area is matched. Counts: `asserted`, `matched`, `accuracy`.
- **Encounter precision** (on `EncounterMetrics`): over matched areas whose truth asserts encounters — `precision_denominator` = distinct folded extracted names, `precision_matched` = those whose fold appears in the truth area's fold set, `precision`. Name-level, mirroring recall; the recall denominator's meaning is unchanged by construction. Expected landing (measured on the retained sweep-1 caches under full assertion): JN1 ≈ 0.93, JN2 ≈ 0.98 — a near-ceiling family; its band row will matter more than its headline.
- **`ModuleMetrics`** gains required `doors`, `transitions`, `entrances` fields and the extended `EncounterMetrics` — required, no defaults: the scoreboard is a run record and every entry regenerates in this phase (the phase 5 ruling; no CI test loads any committed board). `ByomEntry` takes the same model change with no data migration (the *committed* BYOM board still doesn't exist); the *private* corpus scoreboard's four entries regenerate through the `rescore` tooling below, exactly like the committed three. `corpus_means` and `run_eval.py`'s `_print_metrics` carry the new families.
- **The `rescore` subcommand** — the pinned mechanism for offline regeneration, which the existing tooling cannot perform: `cmd_score --update-scoreboard` validates the whole stale board on load (which required-no-defaults fields make fail) and stamps today's date and version into the run block (which "same runs, re-scored" forbids). `run_eval.py rescore <module-id> --workdir DIR [--corpus DIR]` reads the existing scoreboard as raw JSON (no model validation of the stale shape), re-scores the workdir's caches against current truth, writes the entry with the raw-carried run block verbatim (the run's own date, tokens, cost, model id, package version — the entry stays a record of the run) and a freshly pinned `truth_sha256`, then validates the *entire rebuilt board* through the extended models before saving. This is the offline counterpart the regression rule's "re-score the standing sweep pair" discipline has needed — the match-fold re-score (PR #20) did it by hand; now it's tooling with tests. The amendment records the re-score date and scorer version beside the pair, so the run-block/metrics provenance split is visible.

### 3. Tests

- Truth-model tests: each new validator's accept/reject pairs, the asserted-empty flip (omitted ≠ `[]`), door-contradiction rejection, transition endpoint existence.
- Scorer tests per family over synthetic fixtures: the JN2 collapsed-level shape, the cross-level keyed shape, the level-shaped stub, door dedup and kind-conflict resolution, entrance mismatch, precision with hallucinated names.
- The pinned JN1 CI baseline (`test_jn1_pinned_baseline_over_the_committed_caches`) re-blessed: the new families' exact values are added, and the existing four families' values are byte-identical to today's — the truth-key freeze in work item 4 makes any existing-family movement a scorer bug by construction, and the re-bless comment states exactly that.
- `rescore` subcommand tests: run-block carriage verbatim, stale-board raw-JSON read, whole-board validation on write, refusal on a module with no existing entry.
- The determinism test covers the new families; the full-assertion test extension from work item 1.

### 4. The truth passes — every corpus member, in-phase

- **The freeze.** Existing assertion keys are frozen for the duration of the passes: authoring and verification agents add the new families and touch nothing that exists. A suspected error in an existing assertion is recorded as a finding in the pass report — never edited — and the findings file as a follow-up issue at phase close. This is what makes work item 5's hard check meaningful: with existing keys untouched, any existing-family metric drift on the re-score is a scorer bug by construction, not a truth correction to adjudicate.
- **Committed members** (JN1, JN2, minimod): author `doors` over the ~92 asserted-connection areas, `transitions` and `entrance` per dungeon, and explicit `encounters: []` on every verified-empty area (~105 areas) — all from the printed pages under the `AUTHORING.md` independence line. Authoring agents receive the module PDF's pages (text and renders) and the existing truth file, never a `stages/` directory; the retained workdirs are off-limits to authors. Inline YAML comments on every judgment call, as the conventions require.
- **Adversarial verification**: a fresh-context second agent per member re-checks every new assertion against its cited page and hunts omissions; disagreements resolve against the printed page. `truth_provenance.verified` gains a phase 9 entry per member — appended, not rewritten (JN1/JN2's records honestly predate the adversarial pass; this phase gives the committed corpus its first real one).
- **BYOM members** (all five — the four with retained workdirs, plus AA1): same passes at the BYOM partial-truth posture — new families asserted where the existing truth's page coverage reaches, honestly omitted elsewhere. AA1's pass keeps its truth current against the schema and produces no number (no retained workdir; nothing publishes — the standing withdrawal). PDFs and hashes per `~/Documents/osr-forge-byom/SOURCES.md`; truth files, sidecars, and packets stay in the private corpus directory, aggregate counts only in anything committed.
- **Owner sampling**: the phase 9 assertion delta (per member: the new keys, the judgment-call flags) files as a GitHub issue assigned to the owner, referencing the standing issue #24 — the phase does not wait on it.

### 5. The offline re-score, the band, the amendment

- Score all six committed-corpus retained workdirs against the extended truth: the sweep-1 workdirs through `rescore` (they hold the board entries; run blocks carried verbatim, `truth_sha256` re-pinned), the sweep-2 workdirs through plain `score` with no board write (they have no entries, and `rescore` refuses them by design — their numbers exist for the pair table alone). Hard check first: the four existing families reproduce the committed scoreboard and the phase 7 amendment pair exactly — with the work item 4 freeze in force, any drift is a scorer bug. Then: the new families' bands come from the observed pair spread floored at 0.02, and the amendment records the full pair table, the flip-check dungeon counts, and the re-score date and scorer version, phase 6/7 style.
- Run `rescore` over the four scored BYOM workdirs; the private corpus scoreboard's entries regenerate in place. The committed BYOM board still does not exist (blocked on owner sampling, unchanged by this phase).
- `tools/eval/README.md`: the band table gains rows (door recall, door precision, door kind accuracy, door locked accuracy, transition recall, transition precision, transition kind accuracy, entrance accuracy, encounter precision); the conventions section gains the four families' authoring rules; the "four pinned metric families" phrasing updates everywhere it appears.

### 6. The contingent member — search, verify, join or record

- Candidate criteria, pinned: non-BFRPG layout conventions, freely licensed with a verifiable grant (the two-leg phase 0 pattern: the PDF's own license statement, plus the grant's reliability — consent lists, sole-copyright, or equivalent — checked against citable, dated sources), B/X-compatible keyed dungeon content the truth conventions can express, and small enough that its double sweep stays in single-digit dollars (the JN1/JN2 precedent: ≈ $1.2–1.5 per run).
- On a pass: manifest (pinned `sha256`, `license.verified` write-up on the JN1 template, `truth_provenance`), full truth including the new families, authored + adversarially verified like every member, then its own live double sweep — the phase's only live spend — with its pair recorded in the amendment and its entry on the scoreboard.
- On failure: every evaluated candidate and its disqualifying fact recorded in the amendment and summarized in `tools/eval/README.md` beside the BF1 record; the spec's open question stays open. Either outcome closes the work item — the phase never blocks on it.

### 7. Docs and changelog

- `AUTHORING.md`: the new families' conventions — door assertion rules, the undirected transition convention, asserted-empty encounters, and the entrance rule: assert the entrance the module presents as the primary way in (its introduction's or map's labeled entry). A module genuinely presenting co-equal multiple entrances: BYOM members omit the assertion with an inline comment (the assertion-aware escape); committed members — whose posture asserts entrance on every dungeon, test-enforced — assert the one the module presents first and flag the judgment call inline, where the adversarial pass and owner sampling both see it. A multi-valued form waits for a member that needs it. The encounters-asymmetry paragraph rewritten as resolved; the process list and committed-posture sections updated.
- `docs/evals.md` family list, glossary entries for the new terms of art (asserted-empty, edge-fact seam, dungeon-scoped matching), the spec sentence (spec impact above), and the `[Unreleased]` changelog bullet (the scorer is published package code; its metric surface is user-visible).

## Sequencing

1 (schema + validators) → 2 + 3 (scorer + tests, together — the synthetic fixtures drive the computations) → 4 (truth passes; authoring starts once 1 merges locally since the validators gate authoring output) → 5 (re-score + band + amendment; requires 2 and 4) → 7 (docs land with the implementation PR). 6 (the contingent member) starts any time after 1–3 and must resolve — either way — before the phase closes. The whole phase is one implementation PR after the plan PR, per the loop; the truth passes and re-score happen on the implementation branch so the scoreboard, truth files, and scorer land atomically.

## Definition of done

- The seven-family scorer and the `rescore` tooling merge with the pinned JN1 baseline green at zero network and the existing four families reproducing the standing pair exactly.
- Every corpus member — three committed, five BYOM (AA1 asserted but unscored) — carries the new assertions, adversarially verified, provenance appended; any frozen-key error findings from the passes are filed as a follow-up issue.
- The committed scoreboard and band table carry the new families from the re-scored standing pair; the amendment records the pair and the BYOM re-scores.
- The owner-sampling issue for the phase 9 delta exists and is assigned.
- The contingent-member work item is closed with a join (manifest, truth, double sweep, scoreboard entry) or a record (candidates and disqualifying facts, README note).
- Docs updated as in work item 7; changelog bullet present; `ruff format --check`, `ruff check`, `pyright`, `pytest` green.

## Amendments

- **The truth passes completed over all seven authorable members, adversarially verified with zero refutations (2026-07-31).** Committed: JN1 (57 asserted-empty, 46 doors maps / 21 undirected door pairs, the Manor stair, 14 entrances — 11/11 judgment calls upheld), JN2 (47 / 45 / 17 pairs incl. 5 secret, Reed Marsh's two stair pairs, 6 entrances — 12/12 upheld), minimod (full posture). BYOM at the partial posture: HotO (11 door edges; transitions honestly omitted — its verticals leave the keyed level), B3 (19 door edges, the single trapdoor link with a printed only-way sentence, 40 asserted-empty), B4 (25 door facts incl. 9 secret; the complete 9-link tier chain — 5 stairs, 3 trapdoor, 1 chute; 49 asserted-empty), DCC #81 (11 door edges; transitions honestly omitted — the sixth link descends from an unkeyed corridor, so the complete set is unrepresentable; the five keyed links enumerated in comments for a future pass). Frozen-key findings filed for the follow-up issue, none refuting a phase 9 addition: minimod area 3's connections vs the unlabeled map; JN1 H13/H14 connections vs the printed wall (independently confirmed by both passes); JN2 R5 area 9's connections and the R1 outlook-wing loop; B3 area 57's third door into an unkeyed corridor — the five filed as issue #30. Two comment-level citation nits (DCC, B4) touch comments, not assertions, and are recorded in the manifests' verification entries rather than the issue. Every manifest's `truth_provenance.verified` gained its phase 9 entry, appended.
- **The offline re-score ran 2026-07-31 under the seven-family scorer; the hard check passed on both halves.** All six committed-corpus retained workdirs scored against the extended truth: every existing-family value reproduces the committed scoreboard (sweep 1) and the phase 7 amendment pair (sweep 2) exactly — the truth-key freeze held, and the edge-fact seam refactor is byte-identical. No mode-flip in either half (JN1 14/14, JN2 6/6, minimod 1/1 dungeons, both sweeps). The committed board's three entries regenerated through `rescore` in one invocation, run blocks carried verbatim; zero live spend this phase (the contingent member did not join). The new families, run 1 / run 2:

    | metric | minimod | jn1-chaotic-caves | jn2-monkey-isle | band |
    | --- | --- | --- | --- | --- |
    | door recall | n/a / n/a | 0.5238 / 0.5238 | 0.5882 / 0.7059 | 0.118 |
    | door precision | n/a / n/a | 0.5000 / 0.4783 | 0.5882 / 0.8571 | 0.269 |
    | door kind accuracy | n/a / n/a | 1.0 / 1.0 | 1.0 / 1.0 | 0.02 |
    | door locked accuracy | n/a / n/a | 1.0 / 1.0 | 1.0 / 1.0 | 0.02 |
    | transition recall | n/a / n/a | 1.0 / 1.0 | 1.0 / 1.0 | 0.02 |
    | transition precision | 0.0 / n/a | 1.0 / 0.5 | 1.0 / 1.0 | 0.5 |
    | transition kind accuracy | n/a / n/a | 1.0 / 1.0 | 1.0 / 1.0 | 0.02 |
    | entrance accuracy | 1.0 / 1.0 | 0.9286 / 0.9286 | 0.8333 / 0.8333 | 0.02 |
    | encounter precision | 1.0 / 1.0 | 0.9340 / 0.9346 | 0.9804 / 1.0 | 0.02 |

    The wide door and transition-precision ratio bands are small-denominator arithmetic (17–21 door edges, 1–2 vertical links per member; one spurious claim swings transition precision by 0.5), so for those families the counts are the sensitive regression signal — recorded beside the band table in `tools/eval/README.md`. The pinned JN1 CI baseline blessed the new families over the committed test caches, whose recordings predate phase 6's door extraction: its door row honestly pins 0 extracted against 21 asserted — the recording's age, not a scorer defect.
- **What the yardstick immediately shows — the arc's targets, sharpened (2026-07-31).** Three readings the metrics surface on day one. *Doors*: finding them is the problem, describing them isn't — kind and locked accuracy are 1.0 on every true positive in both sweeps, while recall sits at 0.52–0.71 and precision as low as 0.48; the misses are door facts on edges extraction didn't state or stated door-lessly, exactly the evidence phase 11's map pass adds. *Transitions*: extraction recall is 1.0 on every scoreable link in both sweeps — including Reed Marsh's stairs, which the draft loses — so JN2's zero-transition `adventure.json` is a **synthesis** gap (the survey's level collapse plus geometry's `to_level`-only realization), not an extraction gap; the improvement belongs to survey/geometry work, and the metric will hold extraction's credit fixed while it lands. *Entrance*: the positional heuristic fails on exactly the cases the truth authors flagged as judgment-worthy (JN1's vestibule-fed gallery, JN2's labeled-entry-vs-first-listed R5, B4's map-labeled 1a) — 13/14, 5/6, and 0/1 — a clean pre-change baseline for phase 11's map-proposed entrance.
- **The BYOM four re-scored offline on the private board (single runs, advisory, aggregate-only):** HotO — doors 0.6364/0.6364, transitions unasserted, entrance 1.0, encounter precision 0.7419; B3 — doors 0.8421/0.8421, transitions 1/1 (1.0/1.0), entrance 1.0, precision 0.9429; B4 — doors 0.6250/0.9091 (kind 0.8), transitions 9/9 recall 1.0 / precision 0.9, entrance 0.0, precision 0.9130; DCC #81 — doors 0.2727/0.6000, transitions unasserted, entrance 1.0, precision 0.8824. B4 is the vertical showcase (the full tier chain extracted) and the entrance heuristic's clearest miss; DCC #81 stresses doors exactly as its probe role predicts.
- **The contingent member does not join: every candidate failed the pinned procedure (2026-07-31).** The candidate search (web research plus eyes-on-PDF verification) evaluated: **Paleskin Princess** and **Varanus Lair** (Agarwood Studio, itch.io, free) — B/X-notation pamphlet dungeons whose distribution pages declare CC BY 4.0 (itch's machine-filtered CC catalog corroborates), but whose PDFs (sha256 `f9161042…` and `672ced90…`, 2 pages each) carry **no license statement in the document itself**, only credits; the phase 0 procedure's first leg ("the PDF's own license page states…") cannot be satisfied, and the BF1 precedent is to refuse rather than stretch. **Tomb of the Serpent Kings** (Skerples) — structurally the ideal member (3 levels, 52 keyed rooms) — is CC BY-**NC**-SA 4.0 per its own megapost; NC cannot be cleanly relied on for committed corpus text. Also evaluated and disqualified: Necropolis of Nuromen (OGL with names/locations as Product Identity), Trilemma adventures (CC BY-NC), Dyson Logos (map-only grant), Winter's Daughter / The Vanilla Adventure / Stonehell / Idol of the Orcs (retail), Tomb of Sigyfel (10 rooms, OGL PI pattern), Prison of the Hated Pretender (no license statement anywhere), For Gold & Glory adventures (AD&D 2e clone, no open license). **The identified next avenue**: One Page Dungeon Contest winners satisfy both license legs *by construction* — the contest's submission rules require CC BY-SA 4.0 with the license URL printed on the page — with sole-author copyright; what remains is picking a winner dense enough for the metric families, which is follow-up work, not this phase's. The corpus-diversity open question stays open with that lead recorded.
- **AA1's truth pass is unauthorable, not skipped (2026-07-31).** The plan included AA1 in the truth passes to keep its truth current against the schema. Implementation found the private corpus's `SOURCES.md` carries no source path for AA1 — the owner's withdrawal removed it — so no printed pages exist to author from, and truth authored without the printed page is not truth. The new families are assertion-aware optional keys, so AA1's existing truth file remains schema-valid with them unasserted — honest partial truth, not dangling migration debt. Its file is untouched; the follow-up issue notes it; nothing converts, scores, or publishes (the standing withdrawal).
